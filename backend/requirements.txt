# MedSync CATMS — backend/requirements.txt
# No ORM (SQLAlchemy, etc.) per project convention — asyncpg + raw SQL calling the
# database.md functions directly. See AGENTS.md / architecture.md for the stack rationale.

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Database driver (no ORM) ---
asyncpg==0.30.0

# --- Request/response validation & settings ---
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# --- Auth ---
pyjwt==2.10.1
bcrypt==4.2.1

# --- Misc FastAPI runtime deps ---
python-multipart==0.0.20   # form-data parsing, needed if any route accepts multipart forms

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1               # async test client for FastAPI routes
